name: Kursach

on:
  push:
    branches:
      - asd
  pull_request:
    branches:
      - asd

jobs:
  CheckErrors:
    runs-on: windows-latest

    steps:
    - name: Download Code
      uses: actions/checkout@v2

    - name: Download .Net
      uses: microsoft/setup-msbuild@v1.0.2

    - name: Restore Nuget
      run: nuget restore KP.sln

    - name: Build project
      run: |
        msbuild KP.sln /p:Configuration=Release

    - name: Code Analysys
      run: |
        msbuild KP.sln /p:Configuration=Release /p:RunCodeAnalysis=true